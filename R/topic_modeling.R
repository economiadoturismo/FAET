# Funções para Modelagem de Tópicos

# Exemplo de função para executar LDA
# run_lda_model <- function(dfm_data, k_topics) {
#   lda_model <- topicmodels::LDA(dfm_data, k = k_topics)
#   return(lda_model)
# }

