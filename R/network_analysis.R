# Funções para Análise de Redes Científicas

# Exemplo de função para construir rede de colaboração
# build_collaboration_network <- function(M, network_type = "countries") {
#   M_temp <- bibliometrix::metaTagExtraction(M, Field = "AU_CO", sep = ";") # Exemplo para países
#   Net <- bibliometrix::biblioNetwork(M_temp, analysis = "collaboration", network = network_type, sep = ";")
#   return(Net)
# }

