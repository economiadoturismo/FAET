# Funções para Análise Qualitativa Investigativa (Bardin)

# Exemplo de função para preparar corpus textual
# prepare_text_corpus <- function(M, lang = "pt") {
#   corpus_data <- quanteda::corpus(M$AB)
#   dfm_data <- quanteda::dfm(corpus_data, remove = quanteda::stopwords(lang), remove_punct = TRUE)
#   return(list(corpus = corpus_data, dfm = dfm_data))
# }

