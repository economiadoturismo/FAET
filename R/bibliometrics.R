# Funções para Análise Bibliométrica e Cienciométrica

# Exemplo de função para realizar análise bibliométrica
# run_bibliometric_analysis <- function(M) {
#   results <- bibliometrix::biblioAnalysis(M, sep = ";")
#   return(results)
# }

# Exemplo de função para calcular H-index
# calculate_h_index <- function(M, field = "author") {
#   h_index_results <- bibliometrix::Hindex(M, field = field, sep = ";")
#   return(h_index_results)
# }

